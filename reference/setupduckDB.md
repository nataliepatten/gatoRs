# Used in gators_download() - Setting up local query

Downloading a parquet database containing recordsets that were not
uploaded to GBIF by June 2026, for the desired columns. This function
sets up a local duckDB database to use in database searches.

## Usage

``` r
setupduckDB()
```
