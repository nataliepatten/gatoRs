% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_flagged.R
\name{find_flagged}
\alias{find_flagged}
\title{Locality Cleaning - Find possibly problematic occurrence records}
\usage{
find_flagged(df, interactive = TRUE, remove.zero = TRUE, precision = 2)
}
\arguments{
\item{df}{Data frame of occurrence records returned from \code{gators_download()}.}

\item{interactive}{Default = TRUE. The interactive option allows for a visual display
of possible problematic points and the ability to manually remove these points.
Setting \code{interactive = FALSE} will automatically remove these points from the data frame.}

\item{remove.zero}{Default = "TRUE". Indicates that points at (0.00, 0.00) should be removed.}

\item{precision}{Indicates digits to round coordinates too. Coordinates should be round to match the coordinate uncertainty. Default = 2.}
}
\value{
Return cleaned data frame.
}
\description{
The \code{find_flagged()} function allows you to find and map possible problematic points and
manually inspect and remove these points, if desired. Below is an example of a map
produced by \code{find_flagged()} which displays possible problematic points. When running the function you can
hover over a point to see the record's scientific name, and click on a point to see the record's coordinates.

\figure{find_flagged_map.png}{options: style="width:500px"}

Here is an example of the interactive messages displayed by the function for removing points:
\figure{find_flagged_interactive_msgs.png}
}
\details{
This function requires packages dplyr, CoordinateCleaner, leaflet, and magrittr.
This function requires interactive user input.
}
\examples{
data \%>\% find_flagged(interactive = FALSE)

}
